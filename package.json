{
  "name": "prompt-scorer-root",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w packages/prompt-score && npm run build -w apps/web",
    "test": "npm test -w packages/prompt-score"
  }
}
